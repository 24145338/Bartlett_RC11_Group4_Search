# Core dependencies for tagging and visualization
torch
transformers
spacy
streamlit
Pillow

# Note: You also need the spaCy English model.
# Download it after installing these packages using:
# python -m spacy download en_core_web_sm
